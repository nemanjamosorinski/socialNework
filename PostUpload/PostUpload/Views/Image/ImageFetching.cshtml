@model List<PostUpload.ImageTable>

@{
    ViewBag.Title = "ImageFetching";
}

<h2>ImageFetching</h2>

<p>
    @Html.ActionLink("Upload New", "Upload")
</p>

<table>
    @{
        //int j = 0;
        //for (int i = 0; i < Model.Count(); i+=4)
        //{
            int j = 1;
            @*<tr>*@
                while (@*j < i+4 &&*@ j < Model.Count())
                {
                    <tr>
                        <td>
                            <br/>
                            @Html.DisplayFor(m => Model[j].FileName).ToString()
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <img src="data:image/png;base64,@Convert.ToBase64String(Model[j].ImageData, 0, Model[j].ImageData.Length)" width="400" />
                        </td>
                        <td>
                            <br />
                            <br />
                        </td>
                    </tr>
                    j++;
                }
            @*</tr>*@
        @*}*@
    }
</table>
