@model MvcApplication8.Models.Class1

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Class1</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ImageComment)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ImageComment)
            @Html.ValidationMessageFor(model => model.ImageComment)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ImageMimoType)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ImageMimoType)
            @Html.ValidationMessageFor(model => model.ImageMimoType)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
